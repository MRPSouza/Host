class Helper {
    public static function slugify($text) {
        // Converte texto para URL amigável
    }
    
    public static function sanitize($input) {
        // Limpa inputs
    }
    
    public static function excerpt($text, $length = 100) {
        // Gera resumos de texto
    }
} 